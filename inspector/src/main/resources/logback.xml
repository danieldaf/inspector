<configuration>
    
	<property name="DEFAULT_PATTERN" value="%date %level{5} [%thread] %logger{10} [%line] %msg%n" scope="CONTEXT" />
    
<!--     <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender"> -->
<!--         Ponemos en file el PATH definitivo donde se generara el archivo de log propio de la aplicacion          -->
<!--         <file>/var/log/inspector.log</file> -->
<!--         <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy"> -->
<!--             <fileNamePattern>albumInspector.%d{yyyy-MM-dd}.log.gz</fileNamePattern> -->
<!--             <maxHistory>30</maxHistory> -->
<!--         </rollingPolicy> -->
<!--         <encoder> -->
<!--             <pattern> -->
<!--             <pattern>${DEFAULT_PATTERN}</pattern> -->
<!--             </pattern> -->
<!--         </encoder> -->
<!--     </appender> -->
    
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${DEFAULT_PATTERN}</pattern>
        </encoder>
    </appender>
    
    <logger name="ar.daf.foto.inspector" level="debug"/>
    
    <root level="DEBUG">
       	<appender-ref ref="stdout" />
<!--        	Desactivamos la generacion del arhivo de log propio. Fijate si te es necesario.  -->
<!--        	Para gestionar que te den permisos en la carpeta log para crear el arhivo inspector.log  -->
<!--        	<appender-ref ref="file" /> -->
    </root>
</configuration>